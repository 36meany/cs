const renderApp=()=>{const content=$('#content');if(!content)return;const selected=window.selectedCaseId&&CASES.find(c=>c.id===window.selectedCaseId);if(selected)content.innerHTML=renderCaseDetail(selected);else if(window.currentView==='inventory')content.innerHTML=renderInventoryView();else if(window.currentView==='upgrade')content.innerHTML=renderUpgradeView();else if(window.currentView==='stats')content.innerHTML=renderStatsView();else if(window.currentView==='profile')content.innerHTML=renderProfileView();else content.innerHTML=renderCasesView();document.querySelectorAll('.nav-btn,.mobile-menu-btn').forEach(b=>b.classList.toggle('active',b.dataset.view===window.currentView));economy.updateBalance();bindCasesView();bindInventoryView();bindUpgradeView();bindProfileView();};
const claimDailyReward=()=>{const now=Date.now();if(now-gameState.lastDailyTime<86400000){showToast('Награда уже получена сегодня','warning');return}const day=gameState.dailyRewardDay%DAILY_REWARDS.length,reward=DAILY_REWARDS[day].reward;gameState.dailyRewardDay=day+1;gameState.lastDailyTime=now;stateManager.addCoins(reward);showToast(`🎁 День ${day+1}: +${fmt(reward)} ₽`,'success');renderApp()};
const showDropModal=(item,source)=>{const r=RARITY[item.rarity]||RARITY.common;showModal({title:source?.name||'Получен предмет',content:`<div class="drop-result" style="--rarity:${r.color}"><div class="drop-rarity">${r.name}</div><img src="${skinImage(item)}" alt=""><h2>${esc(item.name)}</h2><span>${item.wear||''} · ₽ ${fmt(item.price)}</span></div>`,actions:[{label:'Оставить',type:'primary'},{label:`Продать за ₽ ${fmt(economy.getItemValue(item))}`,type:'gold',handler:()=>{economy.sellItem(item);renderApp()}}]});if(r.order>=2)particleSystem.confetti(r.order>=4?70:30)};
const initApp=async()=>{stateManager.load();if(!gameState.playerId)stateManager.initPlayer();await initYandexSDK();startAutoSave();renderApp();document.querySelectorAll('[data-view]').forEach(btn=>btn.addEventListener('click',()=>{window.currentView=btn.dataset.view;window.selectedCaseId=null;$('#mobileMenu')?.classList.remove('active');renderApp()}));$('#menuToggle')?.addEventListener('click',()=>$('#mobileMenu').classList.add('active'));$('#menuClose')?.addEventListener('click',()=>$('#mobileMenu').classList.remove('active'));$('#mobileMenu')?.addEventListener('click',e=>{if(e.target.id==='mobileMenu')e.currentTarget.classList.remove('active')});$('#buyCoinsBtn')?.addEventListener('click',()=>showToast('Покупка валюты подключается через платежи Яндекс Игр','warning'))};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',initApp):initApp();
