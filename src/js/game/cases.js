const caseManager={getCase(id){return CASES.find(c=>c.id===id)},getCaseItems(id){const c=this.getCase(id);if(!c)return[];const tier=RARITY[c.rarity].order;const allowed=SAMPLE_SKINS.filter(i=>RARITY[i.rarity].order<=Math.min(4,tier+1));const pool=allowed.filter(i=>i.price<=Math.max(c.price*18,300));const source=pool.length?pool:allowed;return source.slice().sort(()=>Math.random()-.5).slice(0,Math.min(18,source.length))},async openCase(id){const c=this.getCase(id);if(!c||!economy.openCase(id))return null;const items=this.getCaseItems(id);const result=economy.rollCase(items);if(!result)return null;const item=stateManager.addItem(result);gameState.stats.totalOpened++;gameState.stats.totalDropped.push(result.rarity);gameState.stats.mostExpensive=Math.max(gameState.stats.mostExpensive,result.price);if(RARITY[result.rarity].order>RARITY[gameState.stats.highestRarity].order)gameState.stats.highestRarity=result.rarity;if(gameState.stats.totalOpened===1)stateManager.unlockAchievement('first_open');if(gameState.stats.totalOpened===10)stateManager.unlockAchievement('ten_opens');if(RARITY[result.rarity].order>=2)stateManager.unlockAchievement('rare_drop');if(RARITY[result.rarity].order>=3)stateManager.unlockAchievement('mythical_drop');if(RARITY[result.rarity].order>=4){stateManager.unlockAchievement('legendary_drop');particleSystem.confetti(50)}stateManager.addExp(Math.max(10,Math.round(c.price/3)));stateManager.save();sounds[result.rarity==='legendary'?'winLegendary':'winRare']?.();gameplayStop();return{item,caseData:c,items}},checkAchievements(){}};
