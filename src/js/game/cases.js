const caseManager={getCase:id=>CASES.find(c=>c.id===id),getCaseItems(id){const c=this.getCase(id);if(!c)return[];const tier=RARITY[c.rarity].order;const maxPrice=Math.max(c.price*22,70);let pool=SAMPLE_SKINS.filter(i=>RARITY[i.rarity].order<=Math.min(4,tier+1)&&i.price<=maxPrice);if(!pool.length)pool=SAMPLE_SKINS.filter(i=>i.price<=maxPrice);return pool.slice().sort(()=>Math.random()-.5).slice(0,18)},async openCase(id){const c=this.getCase(id);if(!c||!economy.openCase(id))return null;const items=this.getCaseItems(id);const result=economy.rollCase(items);if(!result)return null;const item=stateManager.addItem(result);gameState.stats.totalOpened++;gameState.stats.totalDropped.push(result.rarity);gameState.stats.mostExpensive=Math.max(gameState.stats.mostExpensive,result.price);if(RARITY[result.rarity].order>RARITY[gameState.stats.highestRarity].order)gameState.stats.highestRarity=result.rarity;stateManager.addExp(Math.max(8,Math.round(c.price/3)));if(gameState.stats.totalOpened===1)stateManager.unlockAchievement('first_open');if(gameState.stats.totalOpened===10)stateManager.unlockAchievement('ten_opens');if(RARITY[result.rarity].order>=2)stateManager.unlockAchievement('rare_drop');if(RARITY[result.rarity].order>=3)stateManager.unlockAchievement('mythical_drop');if(RARITY[result.rarity].order>=4){stateManager.unlockAchievement('legendary_drop');particleSystem.confetti(55)}stateManager.save();gameplayStop();return{item,caseData:c,items}}};
