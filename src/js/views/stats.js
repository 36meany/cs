const renderStatsView = () => { const s = inventoryManager.getStats(); const a = achievementManager.getStats(); return `<section class="page-head"><div><span class="eyebrow">ПРОГРЕСС</span><h1>Статистика</h1></div></section><div class="stat-grid"><div class="stat-box"><strong>${fmt(gameState.stats.totalOpened)}</strong><span>Кейсов открыто</span></div><div class="stat-box"><strong>${fmt(s.value)}</strong><span>Инвентарь</span></div><div class="stat-box"><strong>${fmt(gameState.stats.mostExpensive)}</strong><span>Лучший дроп</span></div><div class="stat-box"><strong>${a.unlocked}/${a.total}</strong><span>Достижения</span></div></div><h2>Достижения</h2><div class="achievement-grid">${achievementManager.getAllAchievements().map(x => `<div class="achievement ${x.unlocked ? 'unlocked' : ''}"><b>${x.name}</b><span>${x.description}</span><small>+${x.points} XP</small></div>`).join('')}</div>`; };
