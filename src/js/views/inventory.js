const renderInventoryView=()=>{const items=inventoryManager.sort(window.inventorySort||'price');const total=inventoryManager.getTotalValue();return `<section class="page-head"><div><span class="eyebrow">КОЛЛЕКЦИЯ</span><h1>Инвентарь</h1><p class="text-muted">${items.length} предметов · стоимость ₽ ${fmt(total)}</p></div><button class="btn btn-secondary" id="sellAllBtn">Продать всё</button></section><div class="toolbar"><select id="inventorySort"><option value="price">Сначала дорогие</option><option value="rarity">По редкости</option><option value="recent">Новые</option></select></div>${items.length?`<div class="skin-grid">${items.map(i=>`<div class="inventory-item">${skinCard(i)}<button class="inventory-sell" data-sell-id="${i.id}">Продать за ₽ ${fmt(economy.getItemValue(i))}</button></div>`).join('')}</div>`:'<div class="empty-state"><div class="empty-state-icon">◇</div><div class="empty-state-text">Инвентарь пуст.<br>Открой первый кейс.</div></div>'}`};
const bindInventoryView=()=>{$('#sellAllBtn')?.addEventListener('click',()=>{economy.sellAll();renderApp()});$('#inventorySort')?.addEventListener('change',e=>{window.inventorySort=e.target.value;renderApp()});$$('[data-sell-id]').forEach(b=>b.onclick=()=>{const i=gameState.inventory.find(x=>x.id===Number(b.dataset.sellId));if(i)economy.sellItem(i);renderApp()})};
