const skinImage=(item)=>item.image||assetUrl(item.folder,item.name+'.png');
const skinCard=(item)=>`<article class="skin-card" style="--rarity:${RARITY[item.rarity].color}"><img src="${skinImage(item)}" alt="${esc(item.name)}" loading="lazy" onerror="this.onerror=null;this.closest('.skin-card').classList.add('asset-missing')"><div class="skin-card-name">${esc(item.name)}</div><div class="skin-card-foot"><span>${RARITY[item.rarity].name}</span><strong>₽ ${fmt(item.price)}</strong></div></article>`;
const caseCard=(item)=>`<article class="case-card" data-case-id="${item.id}" style="--case-a:${item.color1};--case-b:${item.color2}"><div class="case-card-glow"></div><img class="case-image" src="${item.icon}" alt="${esc(item.name)}" loading="lazy"><div class="case-card-body"><div class="case-card-name">${esc(item.name)} Case</div><div class="case-card-meta"><span>${RARITY[item.rarity].name}</span><strong>₽ ${fmt(item.price)}</strong></div></div></article>`;
const renderCasesView=()=>{const featured=CASES.slice(0,3);return `<section class="page-head"><div><span class="eyebrow">КЕЙСЕР / DROP HUB</span><h1>Охота за редким дропом</h1><p class="text-muted">Цены и коллекции в рублях. PNG-ассеты загружаются из репозитория.</p></div><button class="btn btn-secondary" id="dailyRewardBtn">🎁 Ежедневная награда</button></section><section class="hero-case"><div><span class="badge badge-primary">ХИТ ДНЯ</span><h2>${esc(featured[0].name)} Case</h2><p>Честный шанс · реальные предметы · мгновенное открытие</p><button class="btn btn-primary" data-case-id="${featured[0].id}">Открыть за ₽ ${fmt(featured[0].price)}</button></div><img src="${featured[0].icon}" alt="" class="hero-case-image"></section><div class="section-title"><h2>Коллекции кейсов</h2><span class="text-muted text-sm">${CASES.length} кейсов</span></div><div class="case-grid">${CASES.map(caseCard).join('')}</div>`};
const renderCaseDetail=(c)=>{const items=caseManager.getCaseItems(c.id);return `<button class="btn btn-ghost" id="backCases">← Все кейсы</button><section class="case-detail"><img src="${c.icon}" class="case-detail-image" alt="${esc(c.name)}"><div><span class="badge badge-primary">${RARITY[c.rarity].name}</span><h1>${esc(c.name)} Case</h1><p class="text-muted">${items.length} возможных предметов · стоимость ₽ ${fmt(c.price)}</p><button class="btn btn-primary" id="openCaseBtn">Открыть за ₽ ${fmt(c.price)}</button></div></section><div class="section-title"><h2>Содержимое кейса</h2></div><div class="skin-grid">${items.map(skinCard).join('')}</div>`};
const bindCasesView=()=>{$$('#content [data-case-id]').forEach(el=>el.onclick=()=>{window.selectedCaseId=el.dataset.caseId;renderApp()});$('#backCases')?.addEventListener('click',()=>{window.selectedCaseId=null;renderApp()});$('#openCaseBtn')?.addEventListener('click',async()=>{const result=await caseManager.openCase(window.selectedCaseId);if(result)showDropModal(result.item,result.caseData);economy.updateBalance();renderApp()});$('#dailyRewardBtn')?.addEventListener('click',claimDailyReward)};
