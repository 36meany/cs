const getUpgradeTarget=()=>window.upgradeTarget&&SAMPLE_SKINS.find(i=>i.name===window.upgradeTarget.name)||window.upgradeTarget;
const renderUpgradeView=()=>{const selected=gameState.inventory.find(i=>i.id===window.upgradeFrom);const target=getUpgradeTarget();const targets=selected?upgradeManager.getUpgradeTargets(selected,24):[];const chance=selected&&target?upgradeManager.calculateChance(economy.getItemValue(selected),target.price):0;return `<section class="page-head"><div><span class="eyebrow">RISK / REWARD</span><h1>Апгрейд</h1><p class="text-muted">Обменяй один предмет на более дорогой. Успех добавляет предмет, провал сжигает входной.</p></div></section><div class="upgrade-layout"><div class="upgrade-slot">${selected?skinCard(selected):'<div class="empty-state">Выбери предмет ниже</div>'}</div><div class="upgrade-center"><div class="upgrade-chance">${(chance*100).toFixed(1)}%</div><span class="text-muted">ШАНС УСПЕХА</span><button class="btn btn-primary" id="runUpgradeBtn" ${selected&&target?'':'disabled'}>Испытать удачу</button></div><div class="upgrade-slot">${target?skinCard(target):'<div class="empty-state">Выбери цель справа</div>'}</div></div><div class="upgrade-panels"><div><h2>Мои предметы</h2><div class="skin-grid compact">${gameState.inventory.map(i=>`<div data-upgrade-from="${i.id}" class="${i.id===window.upgradeFrom?'selected':''}">${skinCard(i)}</div>`).join('')||'<div class="empty-state">Инвентарь пуст</div>'}</div></div><div><h2>Цели</h2><div class="skin-grid compact">${targets.map(i=>`<div data-upgrade-target="${encodeURIComponent(i.name)}">${skinCard(i)}</div>`).join('')||'<div class="empty-state">Сначала выбери входной предмет</div>'}</div></div></div>`};
const bindUpgradeView=()=>{$$('[data-upgrade-from]').forEach(el=>el.onclick=()=>{window.upgradeFrom=Number(el.dataset.upgradeFrom);window.upgradeTarget=null;renderApp()});$$('[data-upgrade-target]').forEach(el=>el.onclick=()=>{window.upgradeTarget=SAMPLE_SKINS.find(i=>i.name===decodeURIComponent(el.dataset.upgradeTarget));renderApp()});$('#runUpgradeBtn')?.addEventListener('click',()=>{const from=gameState.inventory.find(i=>i.id===window.upgradeFrom);const target=getUpgradeTarget();if(!from||!target)return;const result=upgradeManager.executeUpgrade(from,target);gameState.stats.upgrades=(gameState.stats.upgrades||0)+1;if(result.success)showDropModal(result.item,{name:'Апгрейд успешен'});else{sounds.lose?.();showToast('Апгрейд не удался — предмет сгорел','error')}window.upgradeFrom=null;window.upgradeTarget=null;stateManager.save();renderApp()})};
